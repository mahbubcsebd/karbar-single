export default function LandingLayout({ children }) {
    return (
        <>
            <div className="landing-pages">
                <main>{children}</main>
            </div>
        </>
    );
}
